apiVersion: v1
kind: Service
metadata:
  name: client-service
  labels:
    component: web
spec:
  selector:
    component: web
  type: ClusterIP
  ports:
    - name: web
      port: 3050
      targetPort: 3000